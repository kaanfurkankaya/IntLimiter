<Window x:Class="IntLimiter.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="IntLimiter" Height="960" Width="1580" MinHeight="900" MinWidth="1300"
        Icon="Assets/IntLimiter.ico"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,8,4" />
            <Setter Property="MinWidth" Value="90" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,4,8,4" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource ButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource ButtonPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,4,8,4" />
            <Setter Property="MinWidth" Value="80" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="Toggle"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="1"
                                          Focusable="False">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="ComboRoot"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="7">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="28" />
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Margin="9,0,0,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"
                                                                  RecognizesAccessKey="True"
                                                                  Content="{Binding Path=SelectionBoxItem, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}"
                                                                  TextElement.Foreground="{DynamicResource TextBrush}" />
                                                <Path Grid.Column="1"
                                                      Width="8"
                                                      Height="5"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Fill="{DynamicResource MutedTextBrush}"
                                                      Data="M 0 0 L 8 0 L 4 5 Z" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ComboRoot" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Fade">
                                <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="260"
                                        Background="{DynamicResource PopupBackgroundBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="4">
                                    <ScrollViewer CanContentScroll="True">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource PopupBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemRoot" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="{DynamicResource SelectionBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="{DynamicResource SelectionBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupBox}">
                        <Border Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="10">
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Top"
                                           Text="{TemplateBinding Header}"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextBrush}"
                                           Margin="0,0,0,8" />
                                <ContentPresenter />
                            </DockPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource MenuBackgroundBrush}" />
        </Style>
        <Style TargetType="TreeView">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="4,2" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="RowBackground" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource AlternateRowBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderBrush}" />
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="6,2" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource HeaderBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="GridCellTextStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="280" />
        </Grid.RowDefinitions>

        <Menu x:Name="MainMenu" Grid.Row="0" Background="{DynamicResource MenuBackgroundBrush}" Foreground="{DynamicResource TextBrush}">
            <MenuItem x:Name="FileMenuItem" Header="Dosya">
                <MenuItem x:Name="OpenLogFolderMenuItem" Header="Log klasörünü aç" Click="OpenLogFolder_Click" />
                <Separator />
                <MenuItem x:Name="ExitMenuItem" Header="Çıkış" Click="ExitMenuItem_Click" />
            </MenuItem>
            <MenuItem x:Name="ViewMenuItem" Header="Görünüm">
                <MenuItem x:Name="LanguageMenuItem" Header="Dil">
                    <MenuItem x:Name="TurkishMenuItem" Header="Türkçe" IsCheckable="True" Click="TurkishMenuItem_Click" />
                    <MenuItem x:Name="EnglishMenuItem" Header="English" IsCheckable="True" Click="EnglishMenuItem_Click" />
                </MenuItem>
                <MenuItem x:Name="ThemeMenuItem" Header="Tema">
                    <MenuItem x:Name="SystemThemeMenuItem" Header="Sistem" IsCheckable="True" Click="SystemThemeMenuItem_Click" />
                    <Separator />
                    <MenuItem x:Name="LightThemeMenuItem" Header="Aydınlık" IsCheckable="True" Click="LightThemeMenuItem_Click" />
                    <MenuItem x:Name="DarkThemeMenuItem" Header="Karanlık" IsCheckable="True" Click="DarkThemeMenuItem_Click" />
                </MenuItem>
                <Separator />
                <MenuItem x:Name="AutoRefreshMenuItem" Header="Otomatik yenile" IsCheckable="True" Click="AutoRefreshMenuItem_Click" />
                <MenuItem x:Name="RefreshIntervalMenuItem" Header="Yenileme aralığı">
                    <MenuItem x:Name="Refresh1sMenuItem" Header="1 saniye" IsCheckable="True" Click="RefreshIntervalMenuItem_Click" />
                    <MenuItem x:Name="Refresh2sMenuItem" Header="3 saniye" IsCheckable="True" Click="RefreshIntervalMenuItem_Click" />
                    <MenuItem x:Name="Refresh5sMenuItem" Header="5 saniye" IsCheckable="True" Click="RefreshIntervalMenuItem_Click" />
                </MenuItem>
            </MenuItem>
            <MenuItem x:Name="ToolsMenuItem" Header="Seçenekler">
                <MenuItem x:Name="RefreshProcessesMenuItem" Header="Processleri yenile" Click="RefreshProcesses_Click" />
                <MenuItem x:Name="ApplyRulesMenuItem" Header="Kuralları uygula" Click="ApplyRules_Click" />
                <MenuItem x:Name="StopAllMenuItem" Header="Tüm limitleri durdur" Click="StopAll_Click" />
                <Separator />
                <MenuItem x:Name="CopyDiagnosticMenuItem" Header="Tanı raporunu kopyala" Click="CopyDiagnosticReport_Click" />
            </MenuItem>
            <MenuItem x:Name="HelpMenuItem" Header="Yardım">
                <MenuItem x:Name="AboutMenuItem" Header="IntLimiter hakkında" Click="AboutMenuItem_Click" />
            </MenuItem>
        </Menu>

        <Border Grid.Row="1" Margin="12,12,12,0" Padding="10" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Image Source="Assets/IntLimiter.png"
                           Width="24"
                           Height="24"
                           Stretch="Uniform"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           Margin="0,0,10,0" />
                    <TextBlock x:Name="ServiceLabelText" Text="Servis:" FontWeight="SemiBold" />
                    <TextBlock x:Name="ServiceStatusText" Text="Unknown" Margin="6,0,18,0" />
                    <TextBlock x:Name="AdminLabelText" Text="Yönetici:" FontWeight="SemiBold" />
                    <TextBlock x:Name="AdminStatusText" Text="Unknown" Margin="6,0,18,0" />
                    <TextBlock x:Name="ModeLabelText" Text="Mod:" FontWeight="SemiBold" />
                    <TextBlock x:Name="ModeStatusText" Text="Stopped" Margin="6,0,18,0" />
                    <TextBlock x:Name="QueueLabelText" Text="Kuyruk:" FontWeight="SemiBold" />
                    <TextBlock x:Name="QueueStatusText" Text="0 packets" Margin="6,0,0,0" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button x:Name="StartServiceButton" Content="Servisi başlat" Click="StartService_Click" />
                    <Button x:Name="StopServiceButton" Content="Servisi durdur" Click="StopService_Click" />
                    <Button x:Name="RefreshProcessesButton" Content="Processleri yenile" Click="RefreshProcesses_Click" />
                    <Button x:Name="ApplyRulesButton" Content="Kuralları uygula" Click="ApplyRules_Click" />
                    <Button x:Name="DeleteRuleButton" Content="Kuralı sil" Click="DeleteRule_Click" />
                    <Button x:Name="StopAllButton" Content="Tüm limitleri durdur" Click="StopAll_Click" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2" Margin="12,12,12,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="460" />
            </Grid.ColumnDefinitions>

            <GroupBox x:Name="ProcessesGroupBox" Grid.Column="0" Header="Processler" Margin="0,0,12,0">
                <Grid Grid.IsSharedSizeScope="True">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Margin="4,2,4,6" LastChildFill="False">
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                            <TextBlock x:Name="ProcessSearchLabelText" Text="Ara" Margin="0,0,8,0" />
                            <TextBox x:Name="ProcessSearchText" Width="220" TextChanged="ProcessSearchText_TextChanged" />
                            <CheckBox x:Name="ActiveOnlyBox" Content="Sadece aktifler" Margin="10,0,0,0" VerticalAlignment="Center" Checked="ProcessFilter_Changed" Unchecked="ProcessFilter_Changed" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                            <TextBlock x:Name="RateUnitLabelText" Text="Hız birimi" Margin="0,0,8,0" />
                            <ComboBox x:Name="RateUnitBox" Width="105" SelectedIndex="1" SelectionChanged="RateUnitBox_SelectionChanged">
                                <ComboBoxItem Content="B/s" />
                                <ComboBoxItem Content="KB/s" />
                                <ComboBoxItem Content="MB/s" />
                                <ComboBoxItem Content="Kbit/s" />
                                <ComboBoxItem Content="Mbit/s" />
                            </ComboBox>
                        </StackPanel>
                    </DockPanel>
                    <Border Grid.Row="1" Background="{DynamicResource HeaderBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="4,3">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2.2*" SharedSizeGroup="ProcessNameColumn" />
                                <ColumnDefinition Width="90" SharedSizeGroup="PidColumn" />
                                <ColumnDefinition Width="120" SharedSizeGroup="UploadColumn" />
                                <ColumnDefinition Width="130" SharedSizeGroup="DownloadColumn" />
                                <ColumnDefinition Width="2.8*" SharedSizeGroup="PathColumn" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="NameHeaderText" Text="Name" FontWeight="SemiBold" Cursor="Hand" MouseLeftButtonDown="NameHeader_MouseLeftButtonDown" />
                            <TextBlock x:Name="PidHeaderText" Grid.Column="1" Text="PID" FontWeight="SemiBold" />
                            <TextBlock x:Name="UploadHeaderText" Grid.Column="2" Text="Upload" FontWeight="SemiBold" Cursor="Hand" MouseLeftButtonDown="UploadHeader_MouseLeftButtonDown" />
                            <TextBlock x:Name="DownloadHeaderText" Grid.Column="3" Text="Download" FontWeight="SemiBold" Cursor="Hand" MouseLeftButtonDown="DownloadHeader_MouseLeftButtonDown" />
                            <TextBlock x:Name="PathHeaderText" Grid.Column="4" Text="Yol" FontWeight="SemiBold" />
                        </Grid>
                    </Border>
                    <TreeView x:Name="ProcessTree"
                              Grid.Row="2"
                              ItemsSource="{Binding ProcessRows}"
                              SelectedItemChanged="ProcessTree_SelectedItemChanged"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="{x:Type TreeViewItem}">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </TreeView.ItemContainerStyle>
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2.2*" SharedSizeGroup="ProcessNameColumn" />
                                        <ColumnDefinition Width="90" SharedSizeGroup="PidColumn" />
                                        <ColumnDefinition Width="120" SharedSizeGroup="UploadColumn" />
                                        <ColumnDefinition Width="130" SharedSizeGroup="DownloadColumn" />
                                        <ColumnDefinition Width="2.8*" SharedSizeGroup="PathColumn" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" Foreground="{DynamicResource TextBrush}" FontWeight="{Binding FontWeight}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}" />
                                    <TextBlock Grid.Column="1" Text="{Binding PidText}" Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="2" Text="{Binding UploadText}" Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="3" Text="{Binding DownloadText}" Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis" />
                                    <TextBlock Grid.Column="4" Text="{Binding Path}" Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Path}" />
                                </Grid>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </GroupBox>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <GroupBox x:Name="SelectedProcessGroupBox" Header="Seçilen process limiti">
                    <StackPanel Margin="10">
                        <TextBlock x:Name="SelectedProcessNameText" Text="{Binding SelectedProcess.ProcessName, TargetNullValue=Process seçilmedi}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding SelectedProcess.ExecutablePath}" FontSize="11" TextWrapping="Wrap" Foreground="{DynamicResource MutedTextBrush}" Margin="0,4,0,12" />
                        <TextBlock x:Name="ProcessDownloadLimitLabelText" Text="Download limiti" />
                        <DockPanel>
                            <TextBox x:Name="ProcessDownloadLimitText" />
                            <ComboBox x:Name="ProcessDownloadUnitBox" SelectedIndex="0">
                                <ComboBoxItem Content="KB/s" />
                                <ComboBoxItem Content="MB/s" />
                            </ComboBox>
                        </DockPanel>
                        <TextBlock x:Name="ProcessUploadLimitLabelText" Text="Upload limiti" Margin="0,8,0,0" />
                        <DockPanel>
                            <TextBox x:Name="ProcessUploadLimitText" />
                            <ComboBox x:Name="ProcessUploadUnitBox" SelectedIndex="0">
                                <ComboBoxItem Content="KB/s" />
                                <ComboBoxItem Content="MB/s" />
                            </ComboBox>
                        </DockPanel>
                        <CheckBox x:Name="ProcessRuleEnabledBox" Content="Etkin" IsChecked="True" Margin="0,8,0,4" />
                        <Button x:Name="ApplyProcessRuleButton" Content="Seçili process limitini ekle" Click="ApplyProcessRule_Click" HorizontalAlignment="Left" />
                    </StackPanel>
                </GroupBox>

                <GroupBox x:Name="GlobalLimitsGroupBox" Grid.Row="1" Header="Global limitler" Margin="0,12,0,0" MinHeight="220">
                    <StackPanel Margin="10">
                        <TextBlock x:Name="GlobalDownloadLimitLabelText" Text="Global download limiti" />
                        <DockPanel>
                            <TextBox x:Name="GlobalDownloadLimitText" />
                            <ComboBox x:Name="GlobalDownloadUnitBox" SelectedIndex="0">
                                <ComboBoxItem Content="KB/s" />
                                <ComboBoxItem Content="MB/s" />
                            </ComboBox>
                        </DockPanel>
                        <TextBlock x:Name="GlobalUploadLimitLabelText" Text="Global upload limiti" Margin="0,8,0,0" />
                        <DockPanel>
                            <TextBox x:Name="GlobalUploadLimitText" />
                            <ComboBox x:Name="GlobalUploadUnitBox" SelectedIndex="0">
                                <ComboBoxItem Content="KB/s" />
                                <ComboBoxItem Content="MB/s" />
                            </ComboBox>
                        </DockPanel>
                        <WrapPanel Margin="0,12,0,0">
                            <CheckBox x:Name="GlobalRuleEnabledBox" Content="Etkin" IsChecked="True" VerticalAlignment="Center" Margin="0,0,12,0" />
                            <Button x:Name="ApplyGlobalRuleButton" Content="Global limitleri ekle" Click="ApplyGlobalRule_Click" HorizontalAlignment="Left" MinWidth="155" />
                        </WrapPanel>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </Grid>

        <Grid Grid.Row="3" Margin="12,0,12,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="350" />
                <ColumnDefinition Width="460" />
            </Grid.ColumnDefinitions>

            <GroupBox x:Name="RulesGroupBox" Header="Kurallar" Margin="0,0,12,0">
                <DataGrid x:Name="RulesGrid"
                          ItemsSource="{Binding Rules}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserResizeColumns="False"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn x:Name="RuleEnabledColumn" Header="Etkin" Binding="{Binding Enabled, Mode=TwoWay}" Width="70" />
                        <DataGridTextColumn x:Name="RuleNameColumn" Header="Ad" Binding="{Binding Name}" Width="*" IsReadOnly="True" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn x:Name="RuleScopeColumn" Header="Kapsam" Binding="{Binding Scope}" Width="90" IsReadOnly="True" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn x:Name="RuleDirectionColumn" Header="Yön" Binding="{Binding Direction}" Width="90" IsReadOnly="True" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn x:Name="RuleLimitColumn" Header="Limit" Binding="{Binding LimitBytesPerSecond, StringFormat={}{0:N0} B/s}" Width="120" IsReadOnly="True" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn x:Name="RuleProcessColumn" Header="Process" Binding="{Binding ProcessName}" Width="115" IsReadOnly="True" ElementStyle="{StaticResource GridCellTextStyle}" />
                        <DataGridTextColumn x:Name="RulePathColumn" Header="Yol" Binding="{Binding ProcessPath}" Width="*" IsReadOnly="True" ElementStyle="{StaticResource GridCellTextStyle}" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox x:Name="DiagnosticsGroupBox" Grid.Column="1" Header="Tanılama / Test" Margin="0,0,12,0">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="145" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="DiagRuntimeModeLabelText" Text="Çalışma modu" FontWeight="SemiBold" Margin="0,0,0,6" />
                        <TextBlock x:Name="DiagCapturedLabelText" Text="Yakalanan paket" />
                        <TextBlock x:Name="DiagDelayedLabelText" Text="Geciken paket" />
                        <TextBlock x:Name="DiagReinjectedLabelText" Text="Yeniden enjekte" />
                        <TextBlock x:Name="DiagDroppedLabelText" Text="Düşen paket" />
                        <TextBlock x:Name="DiagQueueLabelText" Text="Kuyruk uzunluğu" />
                        <TextBlock x:Name="DiagMappingLabelText" Text="Eşleşme OK/hata" />
                        <TextBlock x:Name="DiagLastErrorLabelText" Text="Son hata" />
                    </StackPanel>
                    <StackPanel Grid.Column="1">
                        <TextBlock x:Name="DiagRuntimeModeText" Text="Unknown" FontWeight="SemiBold" Margin="0,0,0,6" />
                        <TextBlock x:Name="DiagCapturedText" Text="0" />
                        <TextBlock x:Name="DiagDelayedText" Text="0" />
                        <TextBlock x:Name="DiagReinjectedText" Text="0" />
                        <TextBlock x:Name="DiagDroppedText" Text="0" />
                        <TextBlock x:Name="DiagQueueText" Text="0" />
                        <TextBlock x:Name="DiagMappingText" Text="0 / 0" />
                        <TextBlock x:Name="DiagLastErrorText" Text="" TextWrapping="Wrap" />
                    </StackPanel>

                    <WrapPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,8,0,0">
                        <Button x:Name="RefreshDiagnosticsButton" Content="Tanılamayı yenile" Click="RefreshDiagnostics_Click" />
                        <Button x:Name="OpenLogFolderButton" Content="Log klasörünü aç" Click="OpenLogFolder_Click" />
                        <Button x:Name="StopAllLimitsButton2" Content="Tüm limitleri durdur" Click="StopAll_Click" />
                        <Button x:Name="CopyDiagnosticReportButton" Content="Tanı raporunu kopyala" Click="CopyDiagnosticReport_Click" />
                    </WrapPanel>
                </Grid>
            </GroupBox>

            <GroupBox x:Name="LogsGroupBox" Grid.Column="2" Header="Son 20 log satırı">
                <ListBox x:Name="LogsList"
                         ItemsSource="{Binding Logs}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock TextWrapping="Wrap" TextTrimming="CharacterEllipsis">
                                <Run Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}" />
                                <Run Text=" " />
                                <Run Text="{Binding Event}" FontWeight="SemiBold" />
                                <Run Text=" " />
                                <Run Text="{Binding Message}" />
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
